package com.multiplexer.dor_dam.viewmodel

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class DorViewModel(application: Application) : AndroidViewModel(application) {
    init {

    }
}